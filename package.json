{
  "name": "pointer-stream",
  "version": "0.0.0",
  "description": "A tiny stream API for mouse and touch events",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dist": "browserify -t [ babelify --presets [ es2015 ] ] index.es6.js > index.js",
    "prepublish": "npm run dist"
  },
  "author": "james.a.forbes@gmail.com <=> (http://james-forbes.com)",
  "license": "ISC",
  "dependencies": {
    "flyd": "^0.2.2",
    "ramda": "^0.21.0"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0"
  }
}
