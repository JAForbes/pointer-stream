{
  "name": "pointer-stream",
  "version": "0.12.0",
  "description": "A tiny stream API for mouse and touch events",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dist": "browserify lib/index.js | uglifyjs -mc > pointer-stream.min.js",
    "prepublish": "npm run dist"
  },
  "author": "james.a.forbes@gmail.com <=> (http://james-forbes.com)",
  "license": "ISC",
  "dependencies": {
    "flyd": "^0.2.2"
  },
  "devDependencies": {
    "browserify": "^13.0.0",
    "uglify-js": "^2.7.5"
  }
}
